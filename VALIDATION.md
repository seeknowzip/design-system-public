# 공개본 검증 기록

2026-09-08, macOS, Node.js 26.0.0, pnpm 10.19.0에서 확인했습니다. Node.js 최소 요구 버전은 Vite 의존성의 조건에 맞춰 22.12.0 이상으로 안내하며, 최소 버전 자체의 실행은 별도 검증하지 않았습니다.

| 확인                             | 결과                                                              |
| -------------------------------- | ----------------------------------------------------------------- |
| 원본 `node_modules` 없이 새 설치 | 통과                                                              |
| `pnpm install --frozen-lockfile` | 통과                                                              |
| `pnpm build`                     | 7개 패키지 통과                                                   |
| `pnpm test:unit`                 | 37개 파일, 268개 통과, 3개 건너뜀                                 |
| `pnpm lint`                      | 오류 0, 기존 Next.js 연동 코드의 불필요한 eslint-disable 경고 3개 |
| MIT 라이선스                     | 원본과 동일한 전문 보존                                           |
| 배포 소스 검사                   | 개인 경로·브랜드 자산·폰트/이미지 바이너리 제외                   |

로컬 확장의 테스트는 마케팅 영역의 접근성, 통계 목록 의미 구조, 일정 항목의 클릭 여부, 아이콘 조회, 장식용 그라데이션 동작을 포함합니다. 테스트에서 쓰는 숫자와 문구는 예시 데이터입니다.

이 결과는 소스 빌드와 단위 동작에 대한 확인입니다. 실제 소비 앱의 화면·반응형·브라우저 호환성 검증이나 모델별 디자인 품질 비교를 대신하지 않습니다. 에이전트 지침의 경로와 범위는 정적 검토했으며, 새 사용자의 에이전트 세션으로 별도 실습하지는 않았습니다.

## 콘텐츠·덱 통합 검증

같은 날 공개본에 두 스킬을 통합한 후 빌드·268개 단위 테스트·lint를 다시 통과했습니다. 개인 원본 저장소는 수정하지 않았습니다.

- 콘텐츠: 5개 포맷, 안전 영역 34개와 이미지 슬롯 21개를 유지했습니다. 원본 대비 인라인 레이아웃을 보존하고 브랜드·예시 문구와 연결 경로를 바꿨습니다. 브라우저에서 603개 공용 export 로딩, 세이프존 스위치의 off→on 전환, 대표 화면을 확인했습니다. 원본과 공개본의 안내 제목은 모두 16px이며, 기존 템플릿의 타이포 클래스 상태를 그대로 유지했습니다.
- 덱: 55개 슬라이드와 55개 발표자 노트를 유지했습니다. 브라우저의 다음 슬라이드 이동과 대표 화면을 확인했고, 브라우저 PDF 출력의 페이지 객체가 55개임을 확인했습니다.
- 별도 저장소·개인 경로 대신 같은 저장소 내부 상대 경로로 자산을 연결했습니다. 스킬 제작 절차와 검수 기준은 각각 유지했습니다.

이는 통합 연결과 대표 동작의 확인이며, 모든 슬라이드의 시각적 완성도나 새 사용자 에이전트 세션에서의 전체 제작 과정을 평가한 결과는 아닙니다. 편집 가능한 PPTX 변환기는 이 배포본에 포함하지 않습니다.
